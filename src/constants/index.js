const devServer = ``;
const liveServer = ``;

const links = [
  { id: "login", title: "Login", path: "/" },
  { id: "signup", title: "Signup", path: "/create-admin" },
];

export { devServer, liveServer, links };
